{
  "name": "fast-browser",
  "version": "0.0.1",
  "private": true,
  "description": "High-throughput local browser agent with extension-ready core",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "publish:cws": "tsx scripts/publish-to-cws.ts"
  },
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
