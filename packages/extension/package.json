{
  "name": "@fast-browser/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && tsx build.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fast-browser/core": "workspace:*",
    "@fast-browser/adapter-cdp-extension": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "esbuild": "^0.25.10",
    "tsx": "^4.19.0",
    "@types/chrome": "^0.0.270",
    "playwright": "^1.49.0"
  }
}
